# Publishing a package via PMC

## Build the package

### rpm/yum (RedHat) compatible packages

### deb/apt (Debian, Ubuntu) compatible packages

## Sign the package

## Upload the package to PMC

## Add uploaded package(s) to a repository version
